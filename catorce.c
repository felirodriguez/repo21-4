#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[]) {
  int n = atoi (argv[1]);
  int res = 1;
  if (n == 0) printf("%d\n", res);
  else {
    while (n > 0) {
      res *= // SIN TERMINAR
    }
  }
  return 0;
}
